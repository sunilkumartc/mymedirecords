import React from 'react';
import './Spinner.css'; // Import CSS file for spinner styling

const Spinner = () => (
  <div className="spinner-container">
    <div className="spinner"></div>
  </div>
);

export default Spinner;
